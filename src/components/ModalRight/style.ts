import styled from "styled-components";

const Close = styled.button`
  border: none;
  padding: 0;
  outline: none;
`;
const CartContainer = styled.div`
  overflow: scroll;
  width: 385px;
  height: 100%;
`;

export { Close, CartContainer };
